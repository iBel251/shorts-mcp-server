{
  "name": "shorts-mcp-server",
  "version": "1.0.0",
  "description": "Headless remote MCP server wrapping the xAI Imagine API for dark editorial-cartoon animated vertical shorts",
  "type": "module",
  "private": true,
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run build:bundles && tsx watch src/index.ts",
    "build:widgets": "node widgets/build.mjs",
    "build:ui": "node ui/build.mjs",
    "build:bundles": "npm run build:widgets && npm run build:ui",
    "build": "npm run build:bundles && tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "npm run build:bundles && tsc -p tsconfig.check.json && tsc -p ui/tsconfig.json",
    "smoke": "tsx scripts/smoke.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.5",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@supabase/supabase-js": "^2.111.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "ffmpeg-static": "^5.3.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.2",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  },
  "allowScripts": {
    "ffmpeg-static@5.3.0": true,
    "esbuild@0.28.1": true
  }
}
